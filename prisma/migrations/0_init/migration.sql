-- CreateTable
CREATE TABLE "movies" (
    "id" INTEGER,
    "title" VARCHAR(150),
    "genres" VARCHAR(50),
    "year" VARCHAR(50)
);

-- CreateTable
CREATE TABLE "users" (
    "id" INTEGER,
    "email" VARCHAR(50),
    "gender" VARCHAR(50),
    "password" VARCHAR(50),
    "role" VARCHAR(50)
);

